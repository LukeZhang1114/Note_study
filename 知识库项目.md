# 知识库项目
es优化：
es的索引分片 设置为3
副本设置为 0
定期维护方案
凌晨强制合并索引
冷热数据区分
结合时间与访问频率
整体项目实现：

通过接口将文件上传到Minio文件服务器，前端通过kkfileview对minio的预览链接处理支持多文件预览操作
上传的文档通过解读（段落拆分）成单一的条目数据，条目数据会通过提前设置的标签关键字匹配上进行条目数据打标。
拆分并打标的数据由用户进行二次校验完成整个解读流程
搜索基于es+mysql读写分离
mysql设置主从同步（主库用来写数据，从库用来读取数据），仅针对知识库的数据。知识条目
es通过logstash监听从库binlog数据进行同步到es中  监听条目的改动信息  
条目数据为 第几章第几条  条目内容 以及条目关联的标签id
搜索关键字传入到es 获取到具体数据 再通过redis 获取标签具体信息  
每次搜索 都会记录 关键字 匹配的条目id 条目内容数据 同时记录 关键词查询频率
条目id设置 三天过期 条目内容设置半小时过期
定时任务 每天执行 按照查询频率数值进行排序 仅存储30条数据
（同时对比三十条数据 id 是否在mysql中）
重复关键词多次搜索 在每次匹配成功后 延长过期时间 + 3小时
定时任务给30 条数据 按照三个不同频率等级进行过期时间延续
分页使用es中的search_after （不支持跳页 使用 from/size （对查询结果做限制））
使用基础字段组合形成唯一字段
​组合效果​​：
- 首先按搜索相关性排序
- 相关性相同的按发布时间排序
- 前两者都相同的按ID升序（确保绝对唯一）
